# 黑洞新首页接入版

基于 2026-09-17 独立个人作品集。原 App 页面、组件、项目和 34 张影像内容保持不变。

顺序：原黑洞视频 → 新粒子黑洞首页（滚动切换文案）→ 星空衔接 → 原个人介绍 → 原有后续页面。

新增入口封装为 `src/components/OpeningSequence.jsx`。迁入组件位于 `public/felix-opening/`，其集成模式隐藏独立演示版的额外导航卡片，直接衔接原首页。原始上传文件夹未修改。

首次访问默认静音播放视频，可开启声音或跳过。同一标签页再次访问、带锚点的站内返回不重复播放。减少动态效果偏好默认跳过视频。`?intro=play` 可强制预览，`?intro=skip` 可直接进入新首页。

部署：`npm ci` 后执行 `npm run build`，发布完整 `dist/` 文件夹，包含 `intro/`、`felix-opening/` 和 `portfolio/`。不要只上传 index.html。根首页应使用 `/`，不要将 `/portfolio/index.html` 设置为网站首页。视频加载失败仍可进入页面。
