# 本轮恢复为社群网站

页面顺序：有声黑洞序章 → 粒子黑洞首页 → Image Archive 图片墙 → 项目及星空影像馆入口 → 社群联系区。

顶部标识和导航固定，星空影像馆导航指向 `#portfolio-gallery`。删除页面中的重复旧个人介绍、旧导航、求职经历和求职联系区。原社群页恢复到 `/lab`，包含六个交流方向和入群联系方式；返回首页不重播视频。

视频没有右下角按钮。默认尝试有声自动播放；浏览器若拦截，点击画面或按 Enter 开始。Esc 可跳过；媒体加载失败时点击画面进入首页。首页测试链接使用 `/?intro=play`，内部返回使用 `/?intro=skip`。

本说明取代此前说明中的“求职版、无社群、默认静音”等描述。运行 `npm run build` 后发布完整 dist，主机需支持 `/lab` 回退到 index.html。`npm run test:browser` 检查当前社群版，旧 career-browser.py 和 opening-browser.py 仅为历史版本测试。
