import { mountStarTrail } from './star-trail.js';

// The archive owns its world-space galaxy inside the photographic WebGL scene.
// Keep the shared cursor trail, without a second, independently moving wallpaper.
mountStarTrail();
